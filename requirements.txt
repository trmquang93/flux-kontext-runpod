# FLUX.1 Kontext-dev Server Dependencies
# Optimized for RunPod Serverless deployment based on research findings
# Note: runpod, torch, transformers, diffusers, accelerate, safetensors, and huggingface_hub 
# are installed in Dockerfile for proper dependency resolution

# Web framework for FastAPI mode (optional)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
aiofiles>=23.0.0

# HTTP and networking
requests>=2.31.0
websocket-client>=1.6.4

# Image processing (opencv-python-headless preferred for serverless)
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# Scientific computing
numpy>=1.24.0
scipy>=1.11.0

# System monitoring for entrypoint validation
psutil>=5.9.0

# FLUX.1 Kontext-dev specific dependencies
controlnet-aux>=0.4.0
invisible-watermark>=0.2.0
omegaconf>=2.3.0
einops>=0.7.0

# API utilities
pydantic>=2.0.0
python-jose[cryptography]>=3.3.0

# Logging and monitoring
structlog>=23.0.0